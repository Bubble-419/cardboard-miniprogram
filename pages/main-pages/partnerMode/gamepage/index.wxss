.container {
  height: 100vh;
  background: #fafafa;
  display: flex;
  flex-direction: column;
  overflow: hidden;
  box-sizing: border-box;
}

/* 历史回顾：一屏定高，禁止整页滚动；灵感栏贴底并可随键盘上移 */
.container-history-review {
  height: 100vh;
  max-height: 100vh;
  overflow: hidden;
}

.container-history-review .page-body {
  flex: 1;
  min-height: 0;
  overflow: hidden;
}

.container-history-review .card-area {
  flex: 1;
  min-height: 0;
}

.container-history-review .inspiration-bar-history {
  margin-top: 12rpx;
  margin-bottom: 0;
  padding-bottom: calc(12rpx + env(safe-area-inset-bottom));
  box-sizing: border-box;
  background: #fafafa;
}

.page-body {
  flex: 1;
  min-height: 0;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

/* Figma #5848:3368 — 顶部内容避开微信胶囊 */
.page-footer {
  flex-shrink: 0;
  padding: 18rpx 0 calc(16rpx + env(safe-area-inset-bottom));
  box-sizing: border-box;
}

.top-bar {
  display: flex;
  flex-direction: column;
  justify-content: flex-start;
  padding-left: 30rpx;
  padding-bottom: 12rpx;
  flex-shrink: 0;
  box-sizing: border-box;
}

.top-bar-inner {
  display: flex;
  align-items: center;
  gap: 16rpx;
  width: 100%;
  box-sizing: border-box;
}

.room-entry {
  border-radius: 9999rpx;
  background: transparent;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  overflow: hidden;
}

.room-entry-icon {
  width: 100%;
  height: 100%;
  display: block;
}

.problem-chip {
  flex: none;
  width: 432rpx;
  max-width: 432rpx;
  min-width: 0;
  min-height: 64rpx;
  padding: 10rpx 20rpx;
  background: #e9f5e9;
  border-radius: 24rpx;
  display: flex;
  align-items: center;
  gap: 16rpx;
  position: relative;
  box-sizing: border-box;
  z-index: 2;
}

.problem-chip-expanded {
  align-items: flex-start;
  z-index: 20;
}

.problem-chip-tappable {
  cursor: pointer;
}

.problem-chip-tappable:active {
  opacity: 0.82;
}

.problem-icon {
  width: 40rpx;
  height: 40rpx;
  flex-shrink: 0;
  margin-top: 2rpx;
}

.problem-text-wrap {
  flex: 1;
  min-width: 0;
  position: relative;
}

.problem-text {
  display: -webkit-box;
  width: 100%;
  font-size: 24rpx;
  color: #747474;
  line-height: 1.2;
  overflow: hidden;
  text-overflow: ellipsis;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
  word-break: break-all;
}

.problem-text-expanded {
  display: block;
  overflow: visible;
  text-overflow: unset;
  -webkit-line-clamp: unset;
  white-space: normal;
}

.problem-text-measure {
  position: absolute;
  left: 0;
  top: 0;
  width: 100%;
  font-size: 24rpx;
  line-height: 1.2;
  word-break: break-all;
  white-space: normal;
  opacity: 0;
  pointer-events: none;
  z-index: -1;
}

.problem-text-empty {
  color: #a8c9a6;
}

.user-section {
  padding: 10rpx 30rpx 0;
  flex-shrink: 0;
}

.user-section-row {
  display: flex;
  flex-direction: row;
  align-items: center;
  gap: 12rpx;
}

.user-section-list {
  flex: 1;
  min-width: 0;
  overflow: hidden;
}

.user-filter-toggle {
  flex-shrink: 0;
  padding: 10rpx 18rpx;
  border-radius: 28rpx;
  background: rgba(94, 193, 89, 0.12);
}

.user-filter-toggle-text {
  font-size: 22rpx;
  color: #5ec159;
  line-height: 1.3;
  white-space: nowrap;
}

.history-review-banner {
  display: flex;
  justify-content: center;
  padding: 4rpx 0 0;
  flex-shrink: 0;
}

.history-review-banner-text {
  font-size: 22rpx;
  color: #7a8a76;
  background: rgba(122, 138, 118, 0.12);
  padding: 4rpx 20rpx;
  border-radius: 20rpx;
}

/* partner-avatar 滚动行：左缘与主卡片对齐（swiper 40rpx + item 8rpx - avatar 8rpx padding） */
.player-avatar-scroll {
  width: 100%;
}

.player-avatar-row {
  display: flex;
  flex-direction: row;
  align-items: flex-start;
  gap: 0;
  padding: 2rpx 0;
  box-sizing: border-box;
  white-space: nowrap;
}

.divider-line {
  height: 2rpx;
  margin: 10rpx 30rpx 8rpx;
  background: #f1f1f1;
  flex-shrink: 0;
}

.card-area {
  flex: 1;
  min-height: 0;
  display: flex;
  flex-direction: column;
  gap: 12rpx;
  padding-bottom: 0;
  overflow: hidden;
}

.card-swiper {
  flex: 1;
  min-height: 0;
  width: 100%;
  height: 100%;
}

.card-swiper-item {
  box-sizing: border-box;
  padding: 0 6rpx;
  height: 100%;
  display: flex;
  flex-direction: column;
  min-height: 0;
}

.card-swiper-item > .game-card,
.card-swiper-item > game-card-timer {
  flex: 1;
  min-height: 0;
  width: 100%;
}

.game-card {
  height: 100%;
  border-radius: 28rpx;
  background: #fff;
  border: 4rpx solid #5ec159;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.game-card-active {
  border-width: 4rpx;
  border-color: #b0e0ae;
}

.game-card-muted {
  border: 4rpx solid #d4e0d4;
  background: #f7faf7;
}

.game-card-no-border {
  border: none;
}

.card-bullet-empty {
  color: #b0b0b0;
}

.card-bullet-empty-tap {
  width: 100%;
  min-height: 56rpx;
  padding: 8rpx 0;
  box-sizing: border-box;
  line-height: 1.5;
}

.card-bullet-empty-tap-hover {
  opacity: 0.65;
}

.action-voice-list {
  padding: 0 0 8rpx;
}

.action-card-wrap {
  position: relative;
  flex: 1;
  min-width: 0;
  min-height: 0;
  width: 100%;
  max-width: 100%;
  height: 100%;
  display: flex;
  flex-direction: column;
  overflow: hidden;
  /* 内容区与底栏按钮共用：左 / 右 / 底 = 30rpx */
  --action-card-inset: 30rpx;
}

.action-card-scroll {
  flex: 1;
  height: 100%;
  min-width: 0;
  min-height: 0;
  width: 100%;
  max-width: 100%;
  overflow: hidden;
  display: flex;
  flex-direction: column;
}

.action-card-inner {
  flex: 1;
  min-width: 0;
  min-height: 0;
  width: 100%;
  max-width: 100%;
  /* 底栏高 60rpx + inset，避免规则区被按钮挡住 */
  padding: 50rpx var(--action-card-inset, 30rpx) calc(var(--action-card-inset, 30rpx) + 76rpx);
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.action-card-inner-express {
  padding-bottom: calc(var(--action-card-inset) + 76rpx);
}

/* 卡片右下角表达入口（出牌玩家）：左 / 右 / 底 = 内容区 inset */
.card-express-corner {
  position: absolute;
  left: var(--action-card-inset, 30rpx);
  right: var(--action-card-inset, 30rpx);
  bottom: var(--action-card-inset, 30rpx);
  z-index: 6;
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: flex-end;
  max-width: none;
  width: auto;
  box-sizing: border-box;
}

.card-express-corner-open {
  left: var(--action-card-inset, 30rpx);
  right: var(--action-card-inset, 30rpx);
  max-width: none;
  width: auto;
  justify-content: stretch;
}

.card-express-corner-open .spectator-express-form,
.card-express-corner-open .spectator-express-composer {
  flex: 1;
  width: 100%;
  min-width: 0;
}

.card-express-corner-with-progress {
  left: var(--action-card-inset, 30rpx);
  right: var(--action-card-inset, 30rpx);
  bottom: var(--action-card-inset, 30rpx);
  max-width: none;
  justify-content: space-between;
  align-items: center;
  gap: 16rpx;
}

/* 出牌阶段旁观：表达钮随打分抽屉高度上移，且不受 clip overflow 影响 */
.spectator-play-express-corner {
  bottom: 80px;
  transition: bottom 240ms cubic-bezier(0.22, 1, 0.36, 1);
}

.action-card-inner-spectator {
  padding: 50rpx 30rpx 220rpx;
  min-height: 100%;
  display: flex;
  flex-direction: column;
  box-sizing: border-box;
}

.action-title-row {
  display: flex;
  align-items: center;
  justify-content: flex-start;
  width: 100%;
  gap: 8rpx;
  min-width: 0;
  box-sizing: border-box;
}

.master-mode-badge {
  flex-shrink: 0;
  width: auto;
  min-width: 0;
  height: 56rpx;
  padding: 0 16rpx;
  background: #5ec159;
  border-radius: 14rpx;
  font-size: 28rpx;
  font-weight: 500;
  color: #fff;
  letter-spacing: -0.06em;
  line-height: 56rpx;
  text-align: center;
  box-sizing: border-box;
  white-space: nowrap;
}

/* Figma #5848:3395 — title + desc gap 17px */
.action-header {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
  width: 100%;
  margin-bottom: 16rpx;
  min-width: 0;
  flex-shrink: 0;
  box-sizing: border-box;
}

.action-header .card-player-round {
  margin-bottom: 0;
}

.action-title-block {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 34rpx;
  width: 100%;
  min-width: 0;
  box-sizing: border-box;
}

/* Figma #5933:8003 — MASTER：徽章+标题同行，与说明间距 7px */
.action-header-master {
  gap: 8rpx;
  align-items: stretch;
}

.action-header-master .action-title-block {
  gap: 14rpx;
  align-items: stretch;
}

.action-title {
  display: block;
  width: 100%;
  text-align: center;
  font-size: 52rpx;
  font-weight: 700;
  color: #5ec159;
  line-height: 1.3;
  letter-spacing: -0.06em;
}

.action-title-master {
  flex: 1;
  min-width: 0;
  width: auto;
  font-size: 36rpx;
  font-weight: 700;
  text-align: left;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  line-height: 1.2;
  letter-spacing: 0.01em;
}

.action-desc {
  display: block;
  width: 100%;
  text-align: center;
  font-size: 24rpx;
  color: rgba(0, 0, 0, 0.4);
  line-height: 1.3;
  letter-spacing: -0.06em;
  box-sizing: border-box;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.action-header-master .action-desc {
  font-size: 24rpx;
  text-align: left;
}

.action-divider {
  height: 2rpx;
  margin: 0 0 16rpx;
  background: #f1f1f1;
  flex-shrink: 0;
}

.action-divider-gap {
  margin-top: 24rpx;
}

/* Figma #5848:3399 — 2×2 grid, gap 8px 10px */
.action-rules-block {
  width: 100%;
  max-width: 100%;
  min-width: 0;
  flex: 1;
  min-height: 0;
  display: flex;
  flex-direction: column;
}

.action-grid {
  flex: 1;
  min-width: 0;
  min-height: 0;
  display: grid;
  grid-template-columns: 1fr 1fr;
  grid-template-rows: 1fr 1fr;
  gap: 16rpx 20rpx;
  width: 100%;
  max-width: 100%;
}

.action-cell {
  display: flex;
  flex-direction: column;
  align-items: stretch;
  gap: 2rpx;
  min-width: 0;
  min-height: 0;
  box-sizing: border-box;
}

.action-illus {
  width: 100%;
  flex: 1;
  min-height: 0;
  height: 0;
  background: #e7e7e7;
  border-radius: 0;
}

.action-cell-label {
  font-size: 24rpx;
  color: rgba(0, 0, 0, 0.4);
  text-align: center;
  line-height: 1.3;
  letter-spacing: -0.06em;
  padding-top: 4rpx;
  flex-shrink: 0;
}

/* 出牌玩家：同步显示打分人数（样式与旁观收起标签一致，不可自评） */
.actor-score-chip {
  flex: 0 1 auto;
  min-width: 0;
  margin: 0;
  height: 60rpx;
  padding: 0 20rpx;
  border-radius: 16rpx;
  background: #5ec159;
  box-sizing: border-box;
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: center;
  white-space: nowrap;
}

.actor-score-chip .star-rating-chip-text {
  overflow: hidden;
  text-overflow: ellipsis;
}

/* 非出牌玩家：匿名聊天 + 打分抽屉（Figma 5960/5964） */
.spectator-card {
  position: relative;
  display: flex;
  flex-direction: column;
  height: 100%;
  min-height: 0;
  box-sizing: border-box;
  /* 与灵感栏 margin 左右 30rpx 对齐；左 / 右 / 底同一 inset */
  --spectator-action-inset: 30rpx;
  --spectator-fab-size: 60rpx;
  --spectator-action-gap: 16rpx;
  /* 与按钮同高，避免垂直居中后再多出空白导致底边偏大 */
  --star-hit-height: 60rpx;
  padding: 36rpx var(--spectator-action-inset) var(--spectator-action-inset);
  overflow: hidden;
}

.score-sheet-backdrop {
  position: absolute;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  z-index: 4;
}

.spectator-bottom-stack {
  position: relative;
  flex-shrink: 0;
  align-self: stretch;
  display: flex;
  flex-direction: row;
  /* 星星与匿名聊天钮垂直居中对齐 */
  align-items: center;
  justify-content: space-between;
  gap: var(--spectator-action-gap);
  width: 100%;
  height: var(--star-hit-height);
  z-index: 8;
  /* 允许按下放大时星尖不被裁切 */
  overflow: visible;
  box-sizing: border-box;
}

.spectator-bottom-stack-collapsed {
  height: var(--star-hit-height);
}

/* chip 打分手势进行中：抬高层级，避免底层卡片 swiper 参与同一指手势 */
.spectator-bottom-stack-scoring {
  z-index: 12;
}

/* 打开匿名输入时盖住打分区 */
.spectator-bottom-stack-composing .spectator-express-slot-flow {
  position: absolute;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  z-index: 10;
  display: flex;
  align-items: center;
  width: auto;
  height: auto;
  padding: 0;
  background: #fff;
  pointer-events: auto;
}

.spectator-bottom-stack-composing .star-rating-shell {
  opacity: 0;
  pointer-events: none;
}

.spectator-express-slot {
  position: relative;
  flex: 0 0 auto;
  z-index: 6;
  width: auto;
  height: auto;
  padding: 0;
  box-sizing: border-box;
  display: flex;
  align-items: center;
  pointer-events: none;
  transition: none;
}

.spectator-express-slot-flow {
  position: relative;
  flex: 0 0 auto;
  z-index: 6;
  width: auto;
  height: auto;
  padding: 0;
  display: flex;
  align-items: center;
}

.spectator-express-slot-animating {
  transition: bottom 240ms cubic-bezier(0.22, 1, 0.36, 1);
}

.spectator-express-slot .spectator-express-form,
.spectator-express-slot .spectator-express-composer,
.spectator-express-slot .spectator-express-send-btn,
.spectator-express-slot .spectator-express-fab-row,
.spectator-express-slot .spectator-express-fab {
  pointer-events: auto;
}

.spectator-express-fab-row {
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: flex-end;
  width: auto;
  height: var(--spectator-fab-size);
  line-height: 0;
}

/* 热区高于星标视觉，减少与卡片横滑冲突；右侧聊天钮走 flex gap，不再靠 padding 硬留白 */
.star-rating-shell {
  position: relative;
  flex: 1 1 auto;
  align-self: stretch;
  width: auto;
  min-width: 0;
  height: var(--star-hit-height);
  overflow: visible;
  box-sizing: border-box;
  padding: 0;
}

.star-rating-shell-collapsed {
  height: var(--star-hit-height);
}

/* 收起态全宽手势层，保证右滑全程落在 catchtouch 上（聊天钮在 shell 外 flex） */
.star-rating-chip-hit {
  position: absolute;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  z-index: 5;
  background: transparent;
}

.star-rating-chip {
  position: absolute;
  left: 0;
  top: 50%;
  bottom: auto;
  margin-top: -30rpx;
  z-index: 2;
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: center;
  height: 60rpx;
  padding: 0 20rpx;
  border-radius: 16rpx;
  background: #5ec159;
  box-sizing: border-box;
  white-space: nowrap;
  opacity: 0;
  visibility: hidden;
  transform: translate3d(-12rpx, 0, 0);
  pointer-events: none;
  transition: opacity 220ms ease, transform 280ms cubic-bezier(0.22, 1, 0.36, 1);
}

.star-rating-shell-collapsed .star-rating-chip {
  opacity: 1;
  visibility: visible;
  transform: translate3d(0, 0, 0);
  /* 手势由 chip-hit 承接，避免窄 chip 断触 */
  pointer-events: none;
}

/* 右滑展开过程中：chip 仅藏视觉；手势由 chip-hit 承接 */
.star-rating-shell-swiping .star-rating-chip {
  opacity: 0;
  visibility: visible;
  pointer-events: none;
  transition: none;
}

.star-rating-shell-swiping .star-rating-body {
  opacity: 1;
  visibility: visible;
  transform: translate3d(0, 0, 0);
  pointer-events: none;
  transition: none;
}

.star-rating-chip-text {
  font-size: 24rpx;
  font-weight: 600;
  color: #fff;
  line-height: 1;
  white-space: nowrap;
}

.star-rating-body {
  position: relative;
  z-index: 1;
  width: 100%;
  height: var(--star-hit-height);
  display: flex;
  align-items: center;
  opacity: 1;
  visibility: visible;
  transform: translate3d(0, 0, 0);
  transform-origin: left center;
  transition: opacity 220ms ease, transform 280ms cubic-bezier(0.22, 1, 0.36, 1);
}

.star-rating-shell-collapsed .star-rating-body {
  opacity: 0;
  visibility: hidden;
  transform: translate3d(-36rpx, 0, 0);
  pointer-events: none;
}

.star-rating-row {
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: center;
  width: 100%;
  height: var(--star-hit-height);
  padding: 0;
  box-sizing: border-box;
}

/* 宿主高度需与组件内 96rpx 星轨一致，否则 overflow 会裁掉星尖 */
.star-rating-row > star-rating {
  display: block;
  flex: 1 1 auto;
  align-self: stretch;
  width: 100%;
  min-width: 0;
  height: var(--star-hit-height);
  line-height: 0;
  overflow: visible;
}

.spectator-express-form {
  width: 100%;
  display: block;
}

.express-form-submit-hidden {
  position: absolute;
  width: 0;
  height: 0;
  padding: 0;
  margin: 0;
  overflow: hidden;
  opacity: 0;
}

.score-sheet-spacer {
  width: 100%;
  flex-shrink: 0;
  pointer-events: none;
}

/* 裁剪窗：高度与 translateY 同步变化，按钮逐渐露出/藏入 */
.score-sheet-clip {
  position: absolute;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 5;
  overflow: hidden;
  pointer-events: auto;
  transition: none;
  will-change: height;
}

.score-sheet-clip-animating {
  transition: height 240ms cubic-bezier(0.22, 1, 0.36, 1);
}

.score-sheet {
  position: absolute;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 5;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 12rpx 16rpx 20rpx;
  background: #fff;
  border-radius: 28rpx 28rpx 0 0;
  box-shadow: 0 -2rpx 16rpx rgba(113, 142, 111, 0.4);
  box-sizing: border-box;
  will-change: transform;
  /* 拖动时跟手：无过渡；吸附时加 .score-sheet-animating */
  transition: none;
}

.score-sheet-animating {
  transition: transform 240ms cubic-bezier(0.22, 1, 0.36, 1);
}

/* 收起动画结束后彻底隐藏按钮，杜绝绿色圆角残留（裁剪层外不可点） */
.score-sheet-collapsed .score-buttons {
  visibility: hidden;
}

.score-sheet-header {
  width: 100%;
  flex-shrink: 0;
}

.score-sheet-handle-wrap {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 4rpx 0 10rpx;
}

.score-sheet-handle {
  width: 68rpx;
  height: 8rpx;
  border-radius: 8rpx;
  background: #aeaeae;
}

.score-sheet-head {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 8rpx 8rpx;
  box-sizing: border-box;
}

.score-sheet .score-buttons {
  width: 100%;
  margin-top: 4rpx;
  position: relative;
  z-index: 2;
  /* 窄屏时优先压缩间距，避免横向溢出或末按钮被挤扁 */
  gap: 8rpx;
  flex-shrink: 0;
}

.spectator-card-head {
  flex-shrink: 0;
  display: flex;
  flex-direction: column;
  align-items: stretch;
  gap: 20rpx;
}

.spectator-card-head .card-player-round {
  margin-bottom: 0;
}

.spectator-card-title {
  display: block;
  text-align: center;
  font-size: 52rpx;
  font-weight: 700;
  color: #5ec159;
  line-height: 1.3;
  letter-spacing: -0.06em;
}

.spectator-card-desc {
  display: block;
  text-align: center;
  font-size: 28rpx;
  color: rgba(0, 0, 0, 0.4);
  line-height: 1.3;
  letter-spacing: -0.06em;
}

.spectator-chat-wrap {
  position: relative;
  z-index: 5;
  flex: 1;
  min-height: 0;
  height: 0;
  width: 100%;
  margin-top: 8rpx;
}

.spectator-chat {
  height: 100%;
  width: 100%;
  padding-bottom: 0;
  box-sizing: border-box;
}

.spectator-chat-empty {
  padding: 80rpx 0;
  display: flex;
  align-items: center;
  justify-content: center;
}

.spectator-chat-empty-text {
  font-size: 26rpx;
  color: #aeaeae;
}

.spectator-chat-row {
  display: flex;
  flex-direction: row;
  align-items: flex-start;
  gap: 16rpx;
  margin-bottom: 20rpx;
  padding-right: 0;
  box-sizing: border-box;
}

.spectator-chat-avatar-wrap {
  position: relative;
  width: 64rpx;
  height: 64rpx;
  flex-shrink: 0;
}

.spectator-chat-avatar {
  width: 64rpx;
  height: 64rpx;
  border-radius: 50%;
  display: block;
  background: #ededed;
}

.spectator-chat-avatar-dot {
  position: absolute;
  right: 0;
  bottom: 0;
  width: 18rpx;
  height: 18rpx;
  border-radius: 50%;
  border: 2rpx solid #ffffff;
  box-sizing: border-box;
}

.spectator-chat-bubble {
  flex: 0 1 auto;
  max-width: 100%;
  min-width: 0;
  padding: 16rpx 20rpx;
  background: #f2f2f2;
  border-radius: 4rpx 18rpx 18rpx 18rpx;
  box-sizing: border-box;
}

.spectator-chat-text {
  font-size: 28rpx;
  color: #333;
  line-height: 1.4;
  word-break: break-word;
}

.summary-express-list {
  margin-top: 8rpx;
  padding-right: 0;
  box-sizing: border-box;
}

.spectator-express-fab {
  width: var(--spectator-fab-size);
  height: var(--spectator-fab-size);
  flex-shrink: 0;
  border-radius: 16rpx;
  background: #5ec159;
  display: flex;
  align-items: center;
  justify-content: center;
  box-sizing: border-box;
  padding: 0;
  line-height: 0;
}

.spectator-express-fab-hover {
  opacity: 0.88;
}

.express-fab-bubble,
.spectator-express-fab-bubble {
  width: 40rpx;
  height: 40rpx;
  flex-shrink: 0;
  display: block;
  margin: 0;
  padding: 0;
}

.action-express-chat-body {
  flex: 1;
  min-height: 360rpx;
  width: 100%;
  display: flex;
  flex-direction: column;
  box-sizing: border-box;
}

.action-express-chat {
  flex: 1;
  height: 100%;
  min-height: 280rpx;
  max-height: 520rpx;
  width: 100%;
  box-sizing: border-box;
}

.action-express-chat-foot {
  flex-shrink: 0;
  display: flex;
  flex-direction: row;
  align-items: center;
  gap: 12rpx;
  margin-top: 16rpx;
  padding-top: 12rpx;
  border-top: 2rpx solid #f1f1f1;
  box-sizing: border-box;
}

.action-express-chat-input {
  flex: 1;
  min-width: 0;
  height: 64rpx;
  padding: 0 20rpx;
  background: #f2f2f2;
  border-radius: 16rpx;
  font-size: 28rpx;
  color: #333;
  box-sizing: border-box;
}

.action-express-chat-locked {
  flex-shrink: 0;
  margin-top: 16rpx;
  padding: 16rpx 0 4rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-top: 2rpx solid #f1f1f1;
}

.action-express-chat-locked-text {
  font-size: 26rpx;
  color: #aeaeae;
}

.discussion-card-body .action-express-chat-body {
  flex: 1;
  min-height: 0;
  height: 100%;
}

.discussion-card-body .action-express-chat {
  max-height: none;
}

.discussion-express-list {
  margin-top: 8rpx;
  box-sizing: border-box;
}

.discussion-express-foot {
  flex-shrink: 0;
  margin: 12rpx 28rpx 0;
  padding-top: 16rpx;
  border-top: 2rpx solid #f1f1f1;
}

.spectator-express-fab-icon {
  width: 60rpx;
  height: 60rpx;
  flex-shrink: 0;
}

.spectator-express-composer {
  position: relative;
  flex: 1;
  display: flex;
  flex-direction: row;
  align-items: center;
  gap: 16rpx;
  min-width: 0;
  width: 100%;
  box-sizing: border-box;
}

.spectator-express-input {
  flex: 1;
  min-width: 0;
  height: 62rpx;
  padding: 0 24rpx;
  background: #f2f2f2;
  border-radius: 18rpx;
  font-size: 28rpx;
  color: #333;
  box-sizing: border-box;
}

.spectator-express-placeholder {
  color: #aeaeae;
}

.spectator-express-send-btn {
  flex: 0 0 60rpx;
  width: 60rpx !important;
  min-width: 60rpx !important;
  max-width: 60rpx !important;
  height: 60rpx;
  padding: 0 !important;
  margin: 0;
  line-height: 0;
  background: transparent;
  border: none;
  border-radius: 16rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  box-sizing: border-box;
}

.spectator-express-send-btn::after {
  border: none;
}

.spectator-express-send-disabled {
  opacity: 0.45;
}

.spectator-express-send-icon {
  width: 60rpx;
  height: 60rpx;
  pointer-events: none;
  display: block;
}

.score-panel {
  flex-shrink: 0;
  margin: 8rpx 0 0;
  padding: 6rpx 14rpx 10rpx;
  background: #fff;
  border: 1rpx solid #f1f5f1;
  border-radius: 28rpx;
  box-shadow: 0 0 8rpx rgba(0, 0, 0, 0.12);
}

/* 浮在出牌并解释面板底部上方 */
.score-panel-float {
  position: absolute;
  left: 20rpx;
  right: 20rpx;
  bottom: 20rpx;
  margin: 0;
  z-index: 5;
  box-shadow: 0 8rpx 24rpx rgba(0, 0, 0, 0.14);
}

game-card-timer {
  display: block;
  height: 100%;
}

.card-header-row {
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
  padding: 24rpx 28rpx 12rpx;
  box-sizing: border-box;
}

.card-section-title {
  font-size: 28rpx;
  font-weight: 600;
  color: #5ec159;
  margin-bottom: 16rpx;
}

.card-section-title-inline {
  margin-bottom: 0;
  flex: 1;
  min-width: 0;
}

.card-earned-stars-wrap {
  flex-shrink: 0;
  width: 100%;
  max-width: 100%;
  padding: 0 24rpx 16rpx;
  box-sizing: border-box;
  overflow: hidden;
}

.review-card-prep {
  opacity: 0;
  transform: translateY(20rpx);
}

.review-card-enter {
  animation: reviewCardEnter 280ms ease forwards;
}

.review-card-fade {
  animation: reviewCardFade 180ms ease forwards;
}

.review-card-switch-next {
  animation: reviewCardSwitchNext 240ms ease forwards;
}

.review-card-switch-prev {
  animation: reviewCardSwitchPrev 240ms ease forwards;
}

.review-card-focus {
  animation: reviewCardFocus 600ms ease both;
  transform-origin: center center;
}

@keyframes reviewCardEnter {
  from {
    opacity: 0;
    transform: translateY(20rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes reviewCardFade {
  from { opacity: 0.35; }
  to { opacity: 1; }
}

@keyframes reviewCardSwitchNext {
  from {
    opacity: 0.4;
    transform: translateX(24rpx);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

@keyframes reviewCardSwitchPrev {
  from {
    opacity: 0.4;
    transform: translateX(-24rpx);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

@keyframes reviewCardFocus {
  0%,
  100% {
    transform: scale(1);
    border-color: #d4e0d4;
  }
  40% {
    transform: scale(1.015);
    border-color: #5ec159;
  }
}

@media (prefers-reduced-motion: reduce) {
  .review-card-enter,
  .review-card-switch-next,
  .review-card-switch-prev,
  .review-card-focus {
    animation: reviewCardFade 180ms ease forwards;
  }
}

.card-player-round {
  display: block;
  text-align: center;
  font-size: 24rpx;
  color: #7a9a78;
  margin-bottom: 8rpx;
}

.card-section-title-gap {
  margin-top: 28rpx;
}

.card-section-block {
  padding: 4rpx 0 8rpx;
  box-sizing: border-box;
}

.card-section-block-gap {
  margin-top: 28rpx;
}

.card-section-flow {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 12rpx;
}

.card-section-flow .card-bullet {
  margin: 0;
}

.card-media-thumb-flow {
  width: 200rpx;
  height: 200rpx;
  border-radius: 12rpx;
  background: #f3f4f6;
  flex-shrink: 0;
}

.card-section-head {
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
  margin-bottom: 12rpx;
}

.card-section-head .card-section-title {
  margin-bottom: 0;
  flex: 1;
  min-width: 0;
}

.card-section-title-tap {
  padding: 4rpx 0;
}

.card-section-add {
  flex-shrink: 0;
  width: 44rpx;
  height: 44rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.card-section-add-icon {
  width: 40rpx;
  height: 40rpx;
}

.card-inline-textarea {
  display: block;
  width: 100%;
  min-height: 88rpx;
  max-height: 360rpx;
  margin: 0;
  padding: 16rpx 0;
  background: transparent;
  border-radius: 0;
  font-size: 28rpx;
  color: #5f5f5f;
  /* 微信 textarea 用倍数行高易裁切，改用固定行高 */
  line-height: 44rpx;
  box-sizing: border-box;
  flex-shrink: 0;
}

.card-inline-textarea-wrap {
  flex-shrink: 0;
  width: 100%;
  min-height: 88rpx;
  margin: 0 0 16rpx;
  box-sizing: border-box;
}

/* 创意点复盘：输入 + 插图入口组成尾部，始终跟在已记录内容之后 */
.closing-compose-tail {
  flex-shrink: 0;
  width: 100%;
  margin-top: 8rpx;
  display: flex;
  flex-direction: column;
  align-items: stretch;
  box-sizing: border-box;
}

.closing-compose-form {
  width: 100%;
  display: block;
}

.closing-compose-tail .card-inline-textarea-wrap {
  margin: 0;
}

.closing-compose-input-row {
  display: flex;
  flex-direction: row;
  align-items: flex-end;
  gap: 16rpx;
}

.closing-compose-input-row .card-inline-textarea {
  flex: 1;
  min-width: 0;
}

.closing-compose-save-btn {
  flex: 0 0 48rpx;
  width: 48rpx !important;
  min-width: 48rpx !important;
  max-width: 48rpx !important;
  height: 48rpx;
  margin: 0 0 20rpx;
  padding: 0 !important;
  line-height: 0;
  background: transparent;
  border: none;
  display: flex;
  align-items: center;
  justify-content: center;
  box-sizing: border-box;
}

.closing-compose-save-btn::after {
  border: none;
}

.closing-compose-save-disabled {
  opacity: 0.35;
}

.closing-compose-save-icon {
  width: 44rpx;
  height: 44rpx;
  pointer-events: none;
  display: block;
}

.closing-compose-tail .closing-deck-capture {
  margin-top: 16rpx;
}

/* 创意点复盘：输入区在内容之后，间距改到上方 */
.closing-card-body > .card-inline-textarea-wrap {
  margin: 8rpx 0 0;
}

.card-inline-textarea-hint {
  min-height: 88rpx;
  padding: 16rpx 0;
}

.card-inline-textarea-active {
  min-height: 96rpx;
  padding: 16rpx 20rpx;
  background: #f7f8f9;
  border-radius: 16rpx;
  color: #111827;
}

.card-record-placeholder {
  color: #b0b0b0;
  font-size: 28rpx;
  line-height: 44rpx;
}

.card-record-placeholder-active {
  color: #d4d4d4;
  font-size: 28rpx;
  line-height: 44rpx;
}

.insert-image-btn {
  display: flex;
  align-items: center;
  gap: 8rpx;
  flex-shrink: 0;
  padding: 4rpx 0;
}

.insert-image-icon {
  width: 32rpx;
  height: 28rpx;
  position: relative;
  box-sizing: border-box;
}

.insert-image-icon::before {
  content: '';
  position: absolute;
  left: 2rpx;
  top: 4rpx;
  width: 28rpx;
  height: 20rpx;
  border: 2rpx solid #5ec159;
  border-radius: 4rpx;
  box-sizing: border-box;
}

.insert-image-icon::after {
  content: '';
  position: absolute;
  left: 10rpx;
  top: 0;
  width: 12rpx;
  height: 8rpx;
  border: 2rpx solid #5ec159;
  border-bottom: none;
  border-radius: 4rpx 4rpx 0 0;
  box-sizing: border-box;
}

.insert-image-text {
  font-size: 28rpx;
  color: #333;
  line-height: 1.3;
}

.card-scroll {
  flex: 1;
  height: 100%;
  min-height: 0;
  width: 100%;
  box-sizing: border-box;
}

.container-history-review .card-scroll {
  height: 0;
}

.card-scroll-inner {
  padding: 0 28rpx 24rpx;
  box-sizing: border-box;
}

.card-bullet-list {
  margin-bottom: 20rpx;
}

.card-bullet {
  font-size: 28rpx;
  color: #5f5f5f;
  line-height: 1.5;
  margin-bottom: 8rpx;
}

.card-turn-row {
  display: flex;
  flex-direction: row;
  align-items: center;
  flex-wrap: wrap;
  gap: 8rpx;
}

.card-photo-wrap {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 24rpx;
  padding: 16rpx 0 24rpx;
}

.card-photo {
  width: 200rpx;
  height: 300rpx;
  border-radius: 8rpx;
  border: 12rpx solid #fff;
  box-shadow: 0 0 12rpx rgba(0, 0, 0, 0.25);
  background: #e7e7e7;
}

.score-panel-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 4rpx;
  padding: 0 4rpx;
}

.score-progress {
  font-size: 24rpx;
  font-weight: 600;
  color: #5ec159;
  line-height: 1.5;
}

.score-buttons {
  display: flex;
  align-items: stretch;
  justify-content: center;
  width: 100%;
  gap: 12rpx;
  box-sizing: border-box;
  overflow: hidden;
}

.score-btn {
  flex: 1 1 0;
  width: 0;
  min-width: 0;
  position: relative;
  border-radius: 21rpx;
  background: #e3fae2;
  box-sizing: border-box;
  overflow: hidden;
}

.score-btn::after {
  content: '';
  display: block;
  padding-bottom: 100%;
}

.score-btn-content {
  position: absolute;
  left: 0;
  top: 0;
  right: 0;
  bottom: 0;
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  box-sizing: border-box;
}

.score-btn-label {
  display: flex;
  flex-direction: row;
  align-items: baseline;
  justify-content: center;
  line-height: 1;
  text-align: center;
}

.score-btn-active {
  background: #5ec159;
}

.score-btn-disabled {
  opacity: 0.45;
}

.score-num {
  font-size: 60rpx;
  font-weight: 600;
  line-height: 1;
  text-align: center;
}

.score-unit {
  font-size: 20rpx;
  font-weight: 600;
  line-height: 1;
  margin-left: 2rpx;
  flex-shrink: 0;
}

.score-btn:not(.score-btn-active) .score-num,
.score-btn:not(.score-btn-active) .score-unit {
  color: #5ec159;
}

.score-btn-active .score-num,
.score-btn-active .score-unit {
  color: #fff;
}

.card-pagination {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 24rpx;
  min-height: 40rpx;
  padding: 4rpx 30rpx;
  position: relative;
  flex-shrink: 0;
  box-sizing: border-box;
  /* 页码绝对定位不撑高；须高于字号，避免被 .card-area overflow:hidden 裁切 */
  overflow: visible;
}

.pagination-dots {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  min-height: 18rpx;
}

.dot {
  width: 18rpx;
  height: 18rpx;
  border-radius: 50%;
  background: #d9d9d9;
  flex-shrink: 0;
}

.dot-lg {
  width: 18rpx;
  height: 18rpx;
}

.dot-md {
  width: 14rpx;
  height: 14rpx;
}

.dot-sm {
  width: 10rpx;
  height: 10rpx;
  opacity: 0.7;
}

.dot-xs {
  width: 8rpx;
  height: 8rpx;
  opacity: 0.5;
}

.dot-active {
  background: #5ec159;
  opacity: 1;
}

.pagination-text {
  position: absolute;
  right: 30rpx;
  top: 50%;
  transform: translateY(-50%);
  font-size: 28rpx;
  line-height: 1.2;
  color: rgba(94, 193, 89, 0.72);
}

/* Figma #5926:2116 — icon / 输入框同高 92rpx，垂直居中对齐 */
.inspiration-bar {
  margin: 18rpx 30rpx 0;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 18rpx;
  flex-shrink: 0;
  position: relative;
  z-index: 60;
  min-height: 92rpx;
  box-sizing: border-box;
  background: #fafafa;
}

/* 收尾阶段：卡片与灵感栏之间留出空隙 */
.inspiration-bar-closing {
  margin-top: 32rpx;
}

/* 无底部操作栏时（非房主）为 Home 指示条留白 */
.inspiration-bar-safe-bottom {
  margin-bottom: calc(24rpx + env(safe-area-inset-bottom));
}

.insp-icon-wrap {
  width: 92rpx;
  height: 92rpx;
  position: relative;
  flex-shrink: 0;
  z-index: 2;
}

.insp-icon-img {
  width: 92rpx;
  height: 92rpx;
}

.insp-dot {
  width: 32rpx;
  height: 32rpx;
  border-radius: 50%;
  background: #5ec159;
  position: absolute;
  right: 0;
  top: 0;
}

/* Figma #5848:3431 — 灵感数量角标 */
.insp-badge {
  min-width: 32rpx;
  height: 32rpx;
  padding: 0 6rpx;
  border-radius: 50%;
  background: #5ec159;
  position: absolute;
  right: 0;
  top: 0;
  z-index: 3;
  display: flex;
  align-items: center;
  justify-content: center;
  box-sizing: border-box;
}

.insp-badge-num {
  font-size: 20rpx;
  font-weight: 500;
  color: #ffffff;
  line-height: 1.3;
  letter-spacing: -0.02em;
}

.inspiration-composer {
  flex: 1;
  min-width: 0;
  height: 92rpx;
  min-height: 92rpx;
  padding: 0 24rpx;
  background: #fff;
  border: 1rpx solid #d7d7d7;
  border-radius: 28rpx;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  justify-content: center;
}

.inspiration-composer-with-photos {
  height: auto;
  min-height: 92rpx;
  padding: 16rpx 24rpx;
}

.inspiration-composer-focused {
  border-color: #d7d7d7;
}

.inspiration-photo-strip {
  width: 100%;
  margin-bottom: 8rpx;
  white-space: nowrap;
}

.inspiration-photo-strip-inner {
  display: inline-flex;
  gap: 12rpx;
}

.inspiration-photo-thumb-wrap {
  position: relative;
  width: 96rpx;
  height: 72rpx;
  flex-shrink: 0;
}

.inspiration-photo-thumb {
  width: 100%;
  height: 100%;
  border-radius: 10rpx;
  background: #eee;
}

.inspiration-photo-remove {
  position: absolute;
  top: -8rpx;
  right: -8rpx;
  width: 32rpx;
  height: 32rpx;
  border-radius: 50%;
  background: rgba(0, 0, 0, 0.55);
  color: #fff;
  font-size: 22rpx;
  line-height: 30rpx;
  text-align: center;
}

/* Figma #5928:13308 — 文案与右侧 icon 左右排布 */
.inspiration-row {
  display: flex;
  flex-direction: row;
  align-items: center;
  gap: 16rpx;
  width: 100%;
  box-sizing: border-box;
}

.inspiration-textarea {
  flex: 1;
  min-width: 0;
  height: 48rpx;
  min-height: 48rpx;
  max-height: 48rpx;
  font-size: 30rpx;
  color: #333;
  line-height: 48rpx;
  letter-spacing: -0.02em;
  padding: 0;
}

.inspiration-placeholder {
  font-size: 30rpx;
  color: #aeaeae;
  line-height: 48rpx;
  letter-spacing: -0.02em;
}

/* Figma：常态加号 / 输入态上箭头，均在输入框内 22px → 44rpx */
.inspiration-action {
  width: 44rpx;
  height: 44rpx;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
}

.inspiration-action-disabled {
  opacity: 0.55;
}

.inspiration-action-icon {
  width: 44rpx;
  height: 44rpx;
}

/* 键盘弹起时隐藏底栏按钮（保留占位，避免回流打断聚焦） */
.page-footer-insp-hidden {
  opacity: 0;
  pointer-events: none;
}

/* 创意点复盘聚焦：藏灵感栏但保留占位，避免回流抢走 textarea 焦点 */
.inspiration-bar-kb-hidden {
  opacity: 0;
  pointer-events: none;
}

/* 房主首次进入：透明热区 + 单行气泡（无视觉蒙版） */
.host-statement-tip-layer {
  position: fixed;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  z-index: 1000;
  background: transparent;
}

.host-statement-tip-bubble {
  position: fixed;
  left: 48rpx;
  right: 48rpx;
  padding: 14rpx 20rpx;
  background: #ffffff;
  border-radius: 16rpx;
  border: 1rpx solid rgba(94, 193, 89, 0.28);
  box-sizing: border-box;
  box-shadow: 0 8rpx 24rpx rgba(20, 40, 28, 0.14);
  z-index: 1002;
}

.host-statement-tip-text {
  display: block;
  font-size: 22rpx;
  line-height: 1.3;
  color: #3a4a40;
  text-align: center;
  font-weight: 500;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.host-statement-tip-arrow {
  position: absolute;
  left: 50%;
  bottom: -10rpx;
  width: 16rpx;
  height: 16rpx;
  margin-left: -8rpx;
  background: #ffffff;
  border-right: 1rpx solid rgba(94, 193, 89, 0.28);
  border-bottom: 1rpx solid rgba(94, 193, 89, 0.28);
  transform: rotate(45deg);
}

/* Figma #5848:3433 — 特殊行动 / 开始表态为两个独立按钮，中间硬分缝 */
.bottom-actions {
  display: flex;
  flex-direction: row;
  align-items: stretch;
  width: 100%;
  box-sizing: border-box;
  padding: 0 30rpx;
}

.special-btn-wrap {
  flex: none;
  width: 162rpx; /* Figma 81px；过窄时原生 button 会溢出盖住右侧按钮 */
  overflow: hidden;
}

/* 非房主当前玩家：仅有特殊行动时铺满底栏，文案横排一行 */
.special-btn-wrap-alone {
  flex: 1;
  width: auto;
  min-width: 0;
}

.special-btn-wrap-alone .special-btn {
  flex-direction: row;
  gap: 0;
  letter-spacing: 0.12em;
}

.special-btn-wrap-alone .special-btn-line {
  display: inline;
  line-height: 1.2;
}

.bottom-actions-gap {
  flex: none;
  width: 24rpx;
  height: 1rpx;
}

.statement-btn-wrap {
  flex: 1;
  min-width: 0;
  overflow: hidden;
}

.special-btn {
  width: 100% !important;
  min-width: 0;
  height: 112rpx;
  margin: 0;
  padding: 0;
  border: none;
  border-radius: 28rpx;
  background: #e9f5e9;
  color: #5ec159;
  font-size: 40rpx;
  font-weight: 600;
  line-height: 1.06em;
  letter-spacing: 0.1em;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  box-sizing: border-box;
}

.special-btn-line {
  display: block;
  line-height: 1.1;
}

.special-btn::after {
  border: none;
}

.special-btn-disabled,
.special-btn[disabled] {
  background: #e7e7e7 !important;
  color: #c1c1c1 !important;
  opacity: 1 !important;
}

/* Figma #5933:8010 — 已使用：底色不变，文案 50% 透明 + 绿色勾选叠加 */
.special-btn-used {
  position: relative;
  background: #e9f5e9;
  color: rgba(94, 193, 89, 0.5);
  box-sizing: border-box;
}

.special-btn-used-check {
  position: absolute;
  left: 50%;
  top: 50%;
  width: 64rpx;
  height: 50rpx;
  transform: translate(-50%, -50%);
  pointer-events: none;
}

.statement-btn {
  width: 100% !important;
  min-width: 0;
  height: 112rpx;
  margin: 0;
  padding: 0;
  border: none;
  border-radius: 28rpx;
  background: #c0c0c0;
  color: #ffffff;
  font-size: 60rpx;
  font-weight: 400;
  letter-spacing: 0.0667em;
  display: flex;
  align-items: center;
  justify-content: center;
  box-sizing: border-box;
}

.statement-btn::after {
  border: none;
}

.statement-btn[disabled] {
  background: #c0c0c0 !important;
  color: #ffffff !important;
  opacity: 1 !important;
}

.statement-btn-enabled {
  background: #5ec159 !important;
  color: #fff !important;
  opacity: 1 !important;
}

.statement-btn-enabled:active {
  opacity: 0.9 !important;
}

.statement-btn-loading {
  background: #5ec159 !important;
  color: #fff !important;
  opacity: 0.85 !important;
  pointer-events: none;
}

.discussion-card-body {
  position: relative;
  flex: 1;
  height: 100%;
  min-height: 0;
  display: flex;
  flex-direction: column;
  /* 水平内边距放在 header / scroll 内容上，避免滚动条被挤离卡片右缘 */
  padding: 24rpx 0 20rpx;
  box-sizing: border-box;
  overflow: hidden;
}

.discussion-card-body .discussion-scroll {
  padding-bottom: 88rpx;
  box-sizing: border-box;
}

.discussion-header-row {
  position: relative;
  display: flex;
  flex-direction: row;
  align-items: flex-start;
  justify-content: center;
  width: 100%;
  flex-shrink: 0;
  margin-bottom: 16rpx;
  min-height: 60rpx;
  padding: 0 28rpx;
  box-sizing: border-box;
}

.discussion-header-main {
  display: flex;
  flex-direction: column;
  align-items: center;
  min-width: 0;
}

.discussion-header-row .card-player-round {
  margin-bottom: 0;
}

.discussion-header-row .discussion-title {
  margin-bottom: 0;
}

.discussion-title {
  display: block;
  text-align: center;
  font-size: 48rpx;
  font-weight: 700;
  color: #5ec159;
  line-height: 1.3;
  margin-bottom: 16rpx;
}

.discussion-divider {
  height: 2rpx;
  background: #ececec;
  margin: 0 28rpx 12rpx;
  flex-shrink: 0;
}

.discussion-scroll {
  flex: 1;
  height: 100%;
  min-height: 0;
  width: 100%;
  box-sizing: border-box;
}

.discussion-scroll-inner {
  padding: 0 28rpx;
  box-sizing: border-box;
}

.discussion-photo-placeholder {
  width: 280rpx;
  height: 228rpx;
  margin: 24rpx auto;
  border: none;
  border-radius: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  background: transparent;
  box-sizing: border-box;
  padding: 0;
}

.discussion-empty-illus {
  width: 280rpx;
  height: 228rpx;
}

.discussion-hint {
  display: block;
  text-align: center;
  font-size: 26rpx;
  color: #b4b4b4;
  line-height: 1.4;
  margin: 16rpx 0 24rpx;
}

.discussion-report-hint {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
  padding-bottom: 16rpx;
}

.discussion-report-title {
  font-size: 26rpx;
  color: #909090;
}

.discussion-report-sub {
  font-size: 22rpx;
  color: #909090;
}

.end-discussion-btn {
  flex: 1;
  width: 100%;
  height: 112rpx;
  margin: 0;
  padding: 0;
  border: none;
  border-radius: 28rpx;
  background: #5ec159;
  color: #fff;
  font-size: 44rpx;
  letter-spacing: 4rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.end-discussion-btn::after {
  border: none;
}

.end-discussion-btn:active {
  opacity: 0.9;
}

.end-discussion-btn-disabled {
  background: #c1c1c1;
}

.bottom-actions-discussion {
  gap: 16rpx;
}

.end-discussion-btn-secondary {
  background: #ffffff;
  color: #5ec159;
  border: 2rpx solid #5ec159;
  box-sizing: border-box;
}

.end-discussion-btn-loading {
  opacity: 0.85;
  pointer-events: none;
}

.express-dismiss-mask {
  position: absolute;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  z-index: 5;
}

/* 收尾阶段 */
.closing-card-stage {
  flex: 1;
  min-height: 0;
  width: 100%;
  height: 100%;
  padding: 0 56rpx 8rpx;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
}

.closing-card-stage > .game-card {
  flex: 1;
  min-height: 0;
  width: 100%;
}

.closing-card {
  position: relative;
  width: 100%;
  height: 100%;
}

.closing-rune-grid-fill {
  flex: 1;
  height: 0;
  margin-bottom: 0;
}

.closing-rune-grid-fill .closing-rune-col {
  height: 100%;
}

.closing-rune-grid-fill .closing-rune-slot {
  flex: 1;
  min-height: 0;
}

.closing-badge {
  position: absolute;
  left: 24rpx;
  top: 24rpx;
  z-index: 2;
  padding: 8rpx 24rpx;
  background: #5ec159;
  border-radius: 16rpx;
  font-size: 28rpx;
  font-weight: 600;
  color: #fff;
}

.closing-card-body {
  flex: 1;
  height: 100%;
  min-height: 0;
  padding: 88rpx 24rpx 48rpx;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.closing-card-title {
  display: block;
  text-align: center;
  font-size: 48rpx;
  font-weight: 700;
  color: #5ec159;
  margin-bottom: 12rpx;
}

.closing-deck-capture {
  margin-top: 32rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  width: 100%;
  box-sizing: border-box;
}

.closing-deck-capture-inner {
  width: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 24rpx;
}

.closing-deck-capture-frame {
  width: 240rpx;
  height: 180rpx;
  border: 3rpx dashed #adadad;
  border-radius: 12rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  box-sizing: border-box;
  background: #fafafa;
}

.closing-deck-camera-icon {
  width: 102rpx;
  height: 84rpx;
}

.closing-deck-photo-wrap {
  position: relative;
  width: 100%;
  margin: 8rpx 0 4rpx;
  box-sizing: border-box;
}

.closing-deck-photo {
  width: 100%;
  height: auto;
  display: block;
  border-radius: 12rpx;
  background: #e7e7e7;
  box-sizing: border-box;
}

.closing-deck-photo-remove {
  position: absolute;
  top: 12rpx;
  right: 12rpx;
  z-index: 2;
  width: 48rpx;
  height: 48rpx;
  border-radius: 50%;
  background: rgba(0, 0, 0, 0.55);
  color: #fff;
  font-size: 32rpx;
  line-height: 44rpx;
  text-align: center;
  box-sizing: border-box;
}

.closing-text-editable:active {
  opacity: 0.7;
}

.closing-card-desc {
  display: block;
  text-align: center;
  font-size: 28rpx;
  color: rgba(0, 0, 0, 0.4);
  margin-bottom: 16rpx;
}

.closing-divider {
  height: 2rpx;
  background: #ececec;
  margin-bottom: 16rpx;
  flex-shrink: 0;
}

.closing-rune-grid {
  display: flex;
  gap: 16rpx;
  margin-bottom: 16rpx;
  flex-shrink: 0;
}

.closing-rune-col {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.closing-rune-slot {
  background: #e7e7e7;
  border-radius: 8rpx;
  min-height: 120rpx;
  display: flex;
  align-items: flex-end;
  justify-content: center;
  padding: 12rpx;
  box-sizing: border-box;
}

.closing-rune-slot-top {
  min-height: 100rpx;
}

.closing-rune-label {
  font-size: 24rpx;
  color: rgba(0, 0, 0, 0.4);
  text-align: center;
}

/* 内容少时按内容高度收缩，使输入区紧跟内容；内容多时占满剩余高度并内部滚动 */
.closing-scroll {
  flex: 0 1 auto;
  min-height: 0;
  /* 抵消父级水平内边距，让滚动条贴齐卡片内右缘 */
  margin: 0 -24rpx;
  width: auto;
}

.closing-scroll-inner {
  padding: 0 24rpx;
  box-sizing: border-box;
}

.closing-shoot-btn {
  align-self: center;
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  min-width: 240rpx;
  height: 72rpx;
  margin: 0 0 16rpx;
  padding: 0 36rpx;
  background: #5ec159;
  border-radius: 36rpx;
  box-sizing: border-box;
  flex-shrink: 0;
}

.closing-shoot-icon {
  width: 36rpx;
  height: 32rpx;
  position: relative;
  box-sizing: border-box;
}

.closing-shoot-icon::before {
  content: '';
  position: absolute;
  left: 2rpx;
  top: 8rpx;
  width: 32rpx;
  height: 22rpx;
  border: 3rpx solid #fff;
  border-radius: 6rpx;
  box-sizing: border-box;
}

.closing-shoot-icon::after {
  content: '';
  position: absolute;
  left: 11rpx;
  top: 12rpx;
  width: 14rpx;
  height: 14rpx;
  border: 3rpx solid #fff;
  border-radius: 50%;
  box-sizing: border-box;
}

.closing-shoot-text {
  font-size: 32rpx;
  font-weight: 600;
  color: #fff;
  letter-spacing: 2rpx;
}

.closing-photo-hint {
  margin: 0 0 16rpx;
  padding: 16rpx 24rpx;
  background: #d9d9d9;
  border-radius: 8rpx;
  text-align: center;
  font-size: 28rpx;
  font-weight: 700;
  color: #000;
  flex-shrink: 0;
}

.closing-review-round {
  margin-bottom: 28rpx;
}

.closing-review-round:last-child {
  margin-bottom: 16rpx;
}

.closing-review-round-title {
  display: block;
  font-size: 28rpx;
  font-weight: 600;
  color: #5ec159;
  margin-bottom: 12rpx;
}

.closing-review-empty {
  padding: 48rpx 24rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.closing-review-empty-text {
  font-size: 28rpx;
  color: rgba(0, 0, 0, 0.35);
}

.closing-photo-placeholder {
  width: 280rpx;
  height: 228rpx;
  margin: 24rpx auto;
  border: none;
  border-radius: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  background: transparent;
  box-sizing: border-box;
  padding: 0;
}

.closing-next-btn,
.end-brainstorm-btn {
  flex: 1;
  height: 112rpx;
  margin: 0;
  padding: 0;
  border: none;
  border-radius: 28rpx;
  background: #5ec159;
  color: #fff;
  font-size: 44rpx;
  letter-spacing: 4rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.closing-next-btn::after,
.end-brainstorm-btn::after {
  border: none;
}

.closing-btn-disabled {
  background: #c1c1c1;
}

/* ---- 卡片插入文字/图片 ---- */
.action-title-row-with-express {
  display: flex;
  flex-direction: row;
  align-items: center;
  width: 100%;
  min-height: 60rpx;
  box-sizing: border-box;
}

.express-fab-spacer {
  width: 60rpx;
  height: 60rpx;
  flex-shrink: 0;
}

.express-fab-wrap {
  width: 60rpx;
  height: 60rpx;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
}

.action-title-main {
  width: 100%;
  min-width: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
}

.action-title-main-master {
  justify-content: flex-start;
  text-align: left;
  padding-right: 12rpx;
  box-sizing: border-box;
}

.action-title-main .action-title {
  width: auto;
  text-align: center;
}

.action-title-main .action-title-row {
  justify-content: center;
  width: auto;
}

.action-title-main-master .action-title-row {
  justify-content: flex-start;
  width: 100%;
}

.express-fab-btn {
  width: 60rpx;
  height: 60rpx;
  flex-shrink: 0;
  border-radius: 16rpx;
  background: #5ec159;
  display: flex;
  align-items: center;
  justify-content: center;
  box-sizing: border-box;
  padding: 0;
  line-height: 0;
}

.express-fab-btn .express-fab-bubble {
  width: 40rpx;
  height: 40rpx;
  display: block;
  margin: 0;
  padding: 0;
  flex-shrink: 0;
}

.express-btn {
  position: relative;
  z-index: 1;
  flex-shrink: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4rpx;
  padding: 4rpx 8rpx;
}

.express-btn-icon {
  width: 56rpx;
  height: 56rpx;
}

.express-btn-label {
  font-size: 20rpx;
  color: #5ec159;
  line-height: 1.2;
}

.card-media-grid {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  gap: 12rpx;
  margin-top: 16rpx;
}

.card-media-thumb {
  width: 160rpx;
  height: 160rpx;
  border-radius: 12rpx;
  background: #f3f4f6;
}

.card-insert-panel {
  margin-top: 28rpx;
  padding: 20rpx;
  background: #f7f8f9;
  border-radius: 20rpx;
  box-sizing: border-box;
}

.card-insert-toolbar {
  display: flex;
  flex-direction: row;
  align-items: center;
  margin-bottom: 12rpx;
}

.card-insert-photos {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  gap: 12rpx;
  margin-bottom: 12rpx;
}

.card-insert-photo-wrap {
  position: relative;
  width: 120rpx;
  height: 120rpx;
}

.card-insert-photo {
  width: 120rpx;
  height: 120rpx;
  border-radius: 12rpx;
  background: #e5e7eb;
}

.card-insert-photo-remove {
  position: absolute;
  top: -8rpx;
  right: -8rpx;
  width: 36rpx;
  height: 36rpx;
  border-radius: 50%;
  background: rgba(17, 24, 39, 0.7);
  color: #fff;
  font-size: 24rpx;
  line-height: 36rpx;
  text-align: center;
}

.card-insert-row {
  display: flex;
  flex-direction: row;
  align-items: flex-end;
  gap: 12rpx;
}

.card-insert-textarea {
  flex: 1;
  min-height: 64rpx;
  max-height: 200rpx;
  padding: 12rpx 16rpx;
  background: #fff;
  border-radius: 16rpx;
  font-size: 28rpx;
  color: #111827;
  line-height: 1.4;
  box-sizing: border-box;
}

.card-insert-placeholder {
  color: #9ca3af;
}

.card-insert-send {
  width: 64rpx;
  height: 64rpx;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
}

.card-insert-send-disabled {
  opacity: 0.4;
}

.card-insert-send-icon {
  width: 48rpx;
  height: 48rpx;
}

/* ---- 出牌玩家匿名聊天面板 ---- */
.express-chat-panel {
  position: fixed;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  z-index: 1000;
  background: #fff;
  display: flex;
  flex-direction: column;
  box-sizing: border-box;
}

.express-chat-panel-head {
  flex-shrink: 0;
  display: flex;
  flex-direction: row;
  align-items: center;
  padding: 24rpx 20rpx;
  padding-top: calc(24rpx + env(safe-area-inset-top));
  border-bottom: 2rpx solid #f1f1f1;
  box-sizing: border-box;
}

.express-chat-panel-back {
  width: 64rpx;
  font-size: 56rpx;
  color: #333;
  line-height: 1;
  text-align: left;
}

.express-chat-panel-title {
  flex: 1;
  text-align: center;
  font-size: 34rpx;
  font-weight: 600;
  color: #111827;
}

.express-chat-panel-head-spacer {
  width: 64rpx;
}

.express-chat-panel-list {
  flex: 1;
  height: 0;
  padding: 24rpx 28rpx;
  box-sizing: border-box;
}

.express-chat-panel-foot {
  flex-shrink: 0;
  display: flex;
  flex-direction: row;
  align-items: center;
  gap: 12rpx;
  padding: 16rpx 24rpx;
  padding-bottom: calc(16rpx + env(safe-area-inset-bottom));
  border-top: 2rpx solid #f1f1f1;
  box-sizing: border-box;
}

.express-chat-panel-input {
  flex: 1;
  min-width: 0;
  height: 72rpx;
  padding: 0 24rpx;
  background: #f2f2f2;
  border-radius: 18rpx;
  font-size: 28rpx;
  color: #333;
  box-sizing: border-box;
}

.express-chat-panel-locked {
  flex-shrink: 0;
  padding: 24rpx;
  padding-bottom: calc(24rpx + env(safe-area-inset-bottom));
  border-top: 2rpx solid #f1f1f1;
  display: flex;
  align-items: center;
  justify-content: center;
}

.express-chat-panel-locked-text {
  font-size: 26rpx;
  color: #aeaeae;
}
