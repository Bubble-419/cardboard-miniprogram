<view class="container">

  <scroll-view class="main-scroll" scroll-y enhanced show-scrollbar="{{false}}" style="height: {{scrollHeight}}px">
    <view class="main-content" style="padding-top: {{contentPaddingTop}}px">

      <!-- 顶部：TopBar + 情境格 -->
      <view class="header-section">

        <!-- TopBar：房间入口 + 堆叠头像 + 添加按钮 -->
        <view class="topbar">
          <view class="room-entry">
            <image class="room-entry-icon" src="/assets/icons/icon-room-entry.svg" mode="aspectFit" />
          </view>
          <view class="avatar-stack">
            <view
              class="avatar-frame {{item.isMe ? 'avatar-me' : ''}}"
              wx:for="{{avatarList}}"
              wx:key="id"
              style="margin-left: {{index > 0 ? '-16rpx' : '0'}}"
            >
              <image class="avatar-img" src="{{item.avatarImage}}" mode="aspectFill" />
            </view>
          </view>
          <view class="add-btn">
            <image class="add-btn-icon" src="/assets/modeIndex/icon-plus.svg" mode="aspectFit" />
          </view>
        </view>

        <!-- 情境格子（2×2 网格，仅展示值） -->
        <view class="context-grid" wx:if="{{categories.length > 0}}">
          <view class="context-item" wx:for="{{categories}}" wx:key="id">
            <text class="context-value">{{item.name}}</text>
          </view>
        </view>

      </view>

      <!-- 标题区 -->
      <view class="title-section">
        <text class="page-title">选择设计问题</text>
        <text class="page-subtitle">房主可以对设计问题进行选择、编辑</text>
      </view>

      <!-- 设计问题列表 -->
      <view class="problem-list">
        <view
          class="problem-item {{item.selected ? 'selected' : ''}} {{editingProblemId === item.id ? 'editing' : ''}} {{!isHost ? 'readonly' : ''}}"
          wx:for="{{problems}}"
          wx:key="id"
          data-id="{{item.id}}"
          bindtap="selectProblem"
        >
          <!-- 左侧竖线指示器 -->
          <view class="problem-indicator {{item.selected ? 'indicator-selected' : ''}}"></view>

          <!-- 问题文字（含编辑态） -->
          <view class="problem-body">
            <block wx:if="{{isHost && editingProblemId === item.id}}">
              <textarea
                class="problem-textarea"
                value="{{item.text}}"
                maxlength="120"
                style="height: {{textareaHeights[item.id] || 17}}px;"
                data-id="{{item.id}}"
                catchtap="stopPropagation"
                bindinput="onProblemInput"
                bindblur="onProblemBlur"
                bindlinechange="onTextareaLineChange"
              />
            </block>
            <text wx:else class="problem-text">{{item.text}}</text>
          </view>

          <!-- 右侧图标：编辑态勾选 / 选中态箭头 / 普通态占位 -->
          <view class="problem-right">
            <view
              wx:if="{{isHost && editingProblemId === item.id}}"
              class="right-icon-btn"
              catchtap="onSaveEdit"
            >
              <image class="right-icon" src="/assets/icons/icon-problem-confirm.svg" mode="aspectFit" />
            </view>
            <view
              wx:elif="{{isHost && item.selected}}"
              class="right-icon-btn"
              data-id="{{item.id}}"
              catchtap="onEditProblem"
            >
              <image class="right-icon" src="/assets/icons/icon-problem-arrow.svg" mode="aspectFit" />
            </view>
            <image
              wx:elif="{{item.selected}}"
              class="right-icon"
              src="/assets/icons/icon-problem-arrow.svg"
              mode="aspectFit"
            />
            <view wx:else class="right-icon-placeholder"></view>
          </view>
        </view>

        <!-- 空状态 -->
        <view class="empty-state" wx:if="{{problems.length === 0}}">
          <text class="empty-text">等待成员提交设计问题…</text>
        </view>
      </view>

    </view>
  </scroll-view>

  <!-- 底部确认按钮（仅房主可见） -->
  <view class="footer" wx:if="{{isHost}}">
    <view class="confirm-btn" bindtap="confirmSelection">确认问题</view>
  </view>

</view>
