<view class="gct-wrap {{timerActive ? 'gct-wrap-timer-on' : ''}} {{active ? 'gct-wrap-active' : ''}}">
  <canvas
    type="2d"
    id="gctCanvas"
    class="gct-canvas {{borderVisible ? 'gct-canvas-visible' : ''}}"
  ></canvas>
  <view class="gct-inner">
    <slot></slot>
  </view>
</view>
